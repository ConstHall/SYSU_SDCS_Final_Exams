#include <iostream>using namespace std;typedef int Queue_entry;class Queue {public://  Standard Queue methods   Queue();   bool empty() const;   /* Returns true if the Queue is empty, otherwise, returns false.   */   int size();   /* Returns the number of elements in the Queue.   */   void push(const Queue_entry &item);   /*item is pushed into the Queue and it becomes the new rear element.   */   void pop();   /*The front item is removed if the Queue is not empty.      Otherwise, nothing happens.   */   void top(Queue_entry &item) const;   /* The front element is returned by item if the Queue is not empty,       and the Queue remains unchanged.       Nothing happens if the Queue is empty.    */   //  Safety features    ~Queue();   Queue(const Queue &original);   void operator =(const Queue &original);};